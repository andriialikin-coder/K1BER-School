{
  "buildCommand": "cd crm && npm install && npm run build",
  "outputDirectory": "crm/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}